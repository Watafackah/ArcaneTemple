using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelUpgrader : MonoBehaviour
{
   /* void Update()
    {
        #region INITS
        GameObject go = GameObject.FindWithTag("Player");     
        //LevelingSystem script = go.GetComponent<LevelingSystem>();
        //PlayerController script1 = go.GetComponent<PlayerController>();
        EscudoController script2 = go.GetComponent<EscudoController>();
        GameObject go1 = GameObject.FindWithTag("Ally");
        //MyAllyManaguer script3 = go1.GetComponent<MyAllyManaguer>();
        #endregion


        #region MEJORAS
        if (script.nivelPlayer == 0)
        {
            script1.vidaMax = 600;
            script2.escudoTotal = 25;
            script1.jumpForce = 9;
            script1.fireRate = 0.5f;
            script3.fireRate = 0.5f;
        }
        if (script.nivelPlayer == 1)
        {
            script1.vidaMax = 700;
            script2.escudoTotal = 75;
            script1.jumpForce = 10;
            script1.fireRate = 0.45f;
            script3.fireRate = 0.45f;
        }
        if (script.nivelPlayer == 2)
        {
            script1.vidaMax = 800;
            script2.escudoTotal = 100;
            script1.jumpForce = 12;
            script1.fireRate = 0.4f;
            script3.fireRate = 0.4f;
        }
        if (script.nivelPlayer == 3)
        {
            script1.vidaMax = 900;
            script2.escudoTotal = 125;
            script1.jumpForce = 13f;
            script1.fireRate = 0.3f;
            script3.fireRate = 0.3f;
        }
        if (script.nivelPlayer == 4)
        {
            script1.vidaMax = 1000;
            script2.escudoTotal = 150;
            script1.jumpForce = 14;
            script1.fireRate = 0.3f;
            script3.fireRate = 0.3f;
        }
        if (script.nivelPlayer == 5)
        {
            script1.vidaMax = 1100;
            script2.escudoTotal = 200;
            script1.jumpForce = 15;
            script1.fireRate = 0.2f;
            script3.fireRate = 0.3f;
        }
        if (script.nivelPlayer >= 6)
        {
            script1.vidaMax = 1250;
            script2.escudoTotal = 250;
            script1.jumpForce = 16;
            script1.fireRate = 0.16f;
            script3.fireRate = 0.3f;
        }
        #endregion
    }*/
}
